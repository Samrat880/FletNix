{
  "name": "fletnix-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test api.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
